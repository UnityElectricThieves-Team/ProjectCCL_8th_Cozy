---
paths:
  - "**/*.prefab"
---

# Prefab 작업 규칙

## 콜로케이션 컨벤션

**프리팹은 사용하는 에셋과 같은 폴더에 둔다.** 별도의 `Assets/Prefabs/` 같은 중앙 폴더는 만들지 않는다.

- 예: 고양이 프리팹 → `Characters/animals/cat/cat.prefab`
- 이유: 캐릭터 단위로 추가/삭제가 폴더 하나만 건드리면 되고, 에셋 ↔ 프리팹 참조 깨짐을 추적하기 쉽다.
- 예외: 여러 캐릭터/모듈이 공유하는 자산은 `_common/` 같은 공용 폴더에.

이 규칙은 캐릭터 외 영역(UI, 이펙트 등)에도 동일하게 적용한다.

## 머지 충돌 주의

`.prefab`도 `.unity`처럼 YAML 기반이지만 직접 편집하면 깨진다.

- Claude는 프리팹을 직접 텍스트 편집하지 않는다 — Unity Editor에서 사용자가 수정.
- 단순 조회(어떤 컴포넌트가 붙어있는지) 목적의 grep은 OK.

## .meta 짝꿍

`.prefab`을 커밋할 때 `.prefab.meta`도 반드시 함께 커밋. GUID가 빠지면 참조가 깨진다.
